# Installing PowerShell Core

## About
These scripts are used per system to install PowerShell Core<br/>
Just double-click them! You don't need to start it on the terminal (Windows Only)

> [!CAUTION]
> By now, only [Linux script][linux_script] was tested on Ubuntu distribution, and the Windows version.

<!-- Links -->
[linux_script] <_project/build/install_pwsh/linux.sh> "Linux's shell script version to install PowerShell Core"