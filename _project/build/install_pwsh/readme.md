# Installing PowerShell Core

## About
These scripts are used per system to install PowerShell Core<br/>
Just double-click them! You don't need to start it on the terminal (Windows Only)

> [!CAUTION]
> By now, only [Linux script](linux.sh "Linux's shell script version to install PowerShell Core") was tested on Ubuntu using WSL, and the Windows version.