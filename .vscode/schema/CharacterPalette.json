{
	"$schema": "https://json-schema.org/draft/2020-12/schema",

	"title": "ASTHG Character Palettes",
	"description": "Color system for the characters, must include at least the 'normal' color state",
	"type": "object",
	"properties": {
		"normal": {
			"description": "Stores the Normal character color",
			"type": "array",
			"items": { "type": "string" },
			"minItems": 4,
			"maxItems": 4
		},
		"super": {
			"description": "Stores the Super Form character color\nOnly required if `hasSuper` property is `true`",
			"type": "array",
			"items": { "type": "string" },
			"minItems": 0,
			"maxItems": 4
		}
	},
	"required": ["normal"]
}