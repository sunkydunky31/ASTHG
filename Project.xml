<?xml version="1.0" encoding="utf-8"?>
<project xmlns="http://lime.openfl.org/project/1.0.4" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://lime.openfl.org/project/1.0.4 http://lime.openfl.org/xsd/project-1.0.4.xsd">

	<!-- ========================= Application Settings ========================= -->

	<app title="Unnamed Sonic Game" file="ASTHG" version="0.0.7"/> <!-- Main Game -->
	<app company="Sunkydev31" packageName="game.sunkydev.asthg"/> <!-- Comapany Settings -->
	<app main="Main" preloader="flixel.system.FlxPreloader"/> <!-- Internal -->

	<!--Minimum without FLX_NO_GAMEPAD: 11.8, without FLX_NO_NATIVE_CURSOR: 11.2-->
	<set name="SWF_VERSION" value="11.8"/>

	<!-- ============================ Window Settings =========================== -->

	<!-- 426:228 is commom on Retro Sonic games, so why not -->
	<window width="426" height="228" fps="60" background="#000000" hardware="true" vsync="false"/>
	<window background="#FF00FF" if="debug"/>

	<!--HTML5-specific-->
	<window if="html5" resizable="true"/>

	<window if="desktop" orientation="landscape" fullscreen="false" resizable="true"/>
	
	<window if="mobile" orientation="landscape" fullscreen="true" width="0" height="0"/>

	<!-- ===============================  Defines =============================== -->
	
	<define name="MODS_ALLOWED"			unless="html5"/>
	<define name="DISCORD_ALLOWED"		if="cpp"/>
	<define name="TRANSLATIONS_ALLOWED"	/>

	<!-- ============================= Path Settings ============================ -->

	<set name="BUILD_DIR" value="export"/>
	<source path="source"/>
	<assets path="assets/shared" exclude=".mp3" unless="web || flash"/>
	<assets path="assets/shared" exclude=".ogg" if="web || flash"/>
	<assets path="assets/fonts" embed="true"/>
	<assets path="mods" embed="false" type="template"/>

	<!-- =============================== Libraries ============================== -->

	<haxelib name="flixel"/>
	<haxelib name="flixel-addons"/>
	<haxelib name="hxdiscord_rpc" if="DISCORD_ALLOWED"/>
	<haxelib name="hscript" if="MODS_ALLOWED"/> <!-- Only for modding! In game scripts will be only source -->
	<haxelib name="haxeui-core"/>
	<haxelib name="haxeui-openfl"/>
	<haxelib name="haxeui-flixel"/>
	<haxelib name="tjson"/>

	<!--In case you want to use nape with flixel-->
	<!--<haxelib name="nape-haxe4"/>-->

	<!-- ============================== Haxedefines ============================= -->
	
	<!--Enable the Flixel core recording system-->
	<!--<haxedef name="FLX_RECORD"/>-->

	<!--Disable the right and middle mouse buttons-->
	<!--<haxedef name="FLX_NO_MOUSE_ADVANCED"/>-->

	<!--Disable the native cursor API on Flash-->
	<!--<haxedef name="FLX_NO_NATIVE_CURSOR"/>-->

	<!--Optimise inputs, be careful you will get null errors if you don't use conditionals in your game-->
	<haxedef name="FLX_NO_MOUSE" if="mobile"/>
	<haxedef name="FLX_NO_KEYBOARD" if="mobile"/>
	<haxedef name="FLX_NO_TOUCH" if="desktop"/>
	<!--<haxedef name="FLX_NO_GAMEPAD"/>-->

	<!--Disable the Flixel core sound tray-->
	<!--<haxedef name="FLX_NO_SOUND_TRAY"/>-->

	<!--Disable the Flixel sound management code-->
	<!--<haxedef name="FLX_NO_SOUND_SYSTEM"/>-->

	<!--Disable the Flixel core focus lost screen-->
	<!--<haxedef name="FLX_NO_FOCUS_LOST_SCREEN"/>-->

	<!--Disable the Flixel core debugger. Automatically gets set whenever you compile in release mode!-->
	<haxedef name="FLX_NO_DEBUG" unless="debug"/>

	<!--Enable this for Nape release builds for a serious peformance improvement-->
	<haxedef name="NAPE_RELEASE_BUILD" unless="debug"/>

	<!-- Haxe 4.3.0+: Enable pretty syntax errors and stuff. -->
	<!-- pretty (haxeflixel default), indent, classic (haxe compiler default) -->
	<haxedef name="message.reporting" value="pretty"/>

	<!-- ================================= Custom =============================== -->


	<icon path="executable/icon8.png" size="8"/>
	<icon path="executable/icon16.png" size="16"/>
	<icon path="executable/icon_expand.png"/>
</project>
